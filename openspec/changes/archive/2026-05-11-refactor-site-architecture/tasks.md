## 1. Site Shell

- [x] 1.1 提取新的基础文档布局，承接 HTML 骨架、SEO、全局样式和首屏主题初始化逻辑
- [x] 1.2 创建统一站点壳层，集中装配顶部导航、搜索模态、主题控制、CRT 效果、移动端提示和页脚
- [x] 1.3 将首页、归档页和关于页迁移到统一站点壳层，移除重复的公共结构拼装
- [x] 1.4 将标签索引页和标签详情页迁移到统一站点壳层，消除标签页与主站体验分叉

## 2. Routing And Assets

- [x] 2.1 新增共享路径辅助模块，统一首页、文章、归档、标签和静态资源地址生成
- [x] 2.2 将导航激活态判断切换到归一化路径逻辑，验证根路径和子路径部署都能正确高亮
- [x] 2.3 将搜索、favicon 和其他静态资源引用切换到共享路径辅助模块，移除写死的根路径引用

## 3. Content Query Layer

- [x] 3.1 新增共享内容查询模块，统一已发布文章过滤与默认排序逻辑
- [x] 3.2 在共享内容查询模块中实现分类树和标签聚合能力
- [x] 3.3 将 `SideNav` 切换到共享分类树数据源，移除组件内部的本地聚合逻辑
- [x] 3.4 将首页、归档页和标签页切换到共享内容查询模块，确保数据来源一致

## 4. Client Component Boundaries

- [x] 4.1 重构搜索交互路径，使导航与搜索模态通过壳层接口通信而不是依赖 `window` 全局函数
- [x] 4.2 重构播放器组件，改为基于组件根节点初始化并消除全局 `id` 依赖
- [x] 4.3 重构视频组件，改为基于局部容器绑定事件并支持同页多实例安全运行
- [x] 4.4 审查并收敛主题、CRT 和其他全局事件的归属，确保壳层与组件边界清晰

## 5. Validation And Cleanup

- [x] 5.1 为首页、归档页、标签页和文章页建立最小人工验证清单，覆盖导航、搜索、主题和路径行为
- [x] 5.2 删除迁移后不再需要的重复布局代码、重复查询逻辑和遗留路径拼接方式
- [x] 5.3 运行项目检查与构建，确认重构后站点仍可正常静态生成与部署
